$(function () {

    //        var notifier = $.connection.notifications;

    //        notifier.notify = function (e) {
    //            console.log(e);
    //        };

    //$.connection.hub.start();
});
